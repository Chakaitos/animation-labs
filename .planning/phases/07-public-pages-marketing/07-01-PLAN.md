---
phase: 07-public-pages-marketing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/marketing/Hero.tsx
  - components/marketing/ExampleGallery.tsx
  - components/marketing/MarketingHeader.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section displays with auto-playing muted video"
    - "Example gallery shows video thumbnails with style and industry labels"
    - "Navigation header has logo, Pricing link, Login link, and Get Started button"
  artifacts:
    - path: "components/marketing/Hero.tsx"
      provides: "Hero section with headline, video, CTA"
      contains: "autoPlay"
    - path: "components/marketing/ExampleGallery.tsx"
      provides: "Grid of example videos with labels"
      contains: "examples.map"
    - path: "components/marketing/MarketingHeader.tsx"
      provides: "Public page navigation"
      contains: "Get Started"
  key_links:
    - from: "components/marketing/Hero.tsx"
      to: "/signup"
      via: "Link component in CTA button"
      pattern: "href.*signup"
    - from: "components/marketing/MarketingHeader.tsx"
      to: "/pricing"
      via: "Link component"
      pattern: "href.*pricing"
---

<objective>
Create the core marketing components for the public homepage: Hero section with auto-playing video and primary CTA, Example gallery with style/industry labels, and shared navigation header.

Purpose: These components form the foundation for converting visitors to users - hero captures attention with value proposition and video demo, gallery shows product quality, navigation enables discovery.

Output: Three React Server Components ready for homepage assembly.
</objective>

<execution_context>
@/Users/chakaitos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chakaitos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-public-pages-marketing/07-CONTEXT.md
@.planning/phases/07-public-pages-&-marketing**---landing-page-and-pricing/07-RESEARCH.md

# Existing components to reference
@components/ui/button.tsx
@components/ui/card.tsx
@public/logo.svg
</context>

</skills>
# Required Skills

Use these skills at the appropriate times:

| Skill | When to Use |
|-------|-------------|
| `frontend-design` | When building ANY UI components, pages, or visual elements |
| `llm-application-dev:prompt-engineer` | When generating/optimizing prompts |
| `backend-development:api-design-principles` | When designing ANY API endpoints or routes |

</skills>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero section component</name>
  <files>components/marketing/Hero.tsx</files>
  <action>
Create Hero.tsx as a React Server Component with:

1. **Headline section** (price + quality focus per CONTEXT.md):
   - Main heading: "Professional Logo Animations" with accent "At a Fraction of the Cost"
   - Subheading: "$3-5 per video, delivered in 10-15 minutes"
   - Use text-4xl md:text-6xl for responsive sizing

2. **Hero video** (auto-playing demo):
   - Container: aspect-video, max-w-4xl mx-auto, rounded-lg overflow-hidden shadow-2xl
   - Video element with attributes: autoPlay, muted, loop, playsInline (CRITICAL: all required for cross-browser auto-play)
   - Source: /examples/hero-demo.mp4 (placeholder path)
   - Fallback: Show Image from next/image if video fails
   - DO NOT use priority on video poster - video is the primary content

3. **Primary CTA** (centered per CONTEXT.md):
   - Button size="lg" with "Get Started" text
   - Links to /signup using Next.js Link component
   - Centered below video with flex justify-center

4. **Styling**:
   - Section padding: py-16 md:py-24
   - Container: container mx-auto px-4
   - Max width constraint: max-w-6xl mx-auto for content

Import Button from @/components/ui/button, Link from next/link.
  </action>
  <verify>
File exists at components/marketing/Hero.tsx.
TypeScript compiles: npx tsc --noEmit
Contains: autoPlay, muted, loop, playsInline attributes on video element.
Contains: Link to /signup in CTA button.
  </verify>
  <done>
Hero component renders headline, auto-playing video section, and "Get Started" CTA linking to signup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Example Gallery component</name>
  <files>components/marketing/ExampleGallery.tsx</files>
  <action>
Create ExampleGallery.tsx as a React Server Component with:

1. **Examples data** (hardcoded array - Claude's discretion on count, suggest 6 for 3-col grid):
   ```typescript
   const examples = [
     { id: 1, thumbnailUrl: '/examples/modern-tech-thumb.jpg', style: 'Modern', industry: 'Tech Startup' },
     { id: 2, thumbnailUrl: '/examples/cinematic-ecommerce-thumb.jpg', style: 'Cinematic', industry: 'E-commerce' },
     { id: 3, thumbnailUrl: '/examples/minimal-agency-thumb.jpg', style: 'Minimal', industry: 'Creative Agency' },
     { id: 4, thumbnailUrl: '/examples/bold-fitness-thumb.jpg', style: 'Bold', industry: 'Fitness' },
     { id: 5, thumbnailUrl: '/examples/elegant-luxury-thumb.jpg', style: 'Elegant', industry: 'Luxury Brand' },
     { id: 6, thumbnailUrl: '/examples/playful-gaming-thumb.jpg', style: 'Playful', industry: 'Gaming' },
   ]
   ```

2. **Section header**:
   - h2: "See Exactly What You'll Get" (sets expectations per CONTEXT.md)
   - Centered, text-3xl font-bold

3. **Grid layout**:
   - grid md:grid-cols-2 lg:grid-cols-3 gap-6
   - Each card: group cursor-pointer for hover effects

4. **Example card structure**:
   - Image container: relative aspect-video rounded-lg overflow-hidden shadow-md
   - next/image with fill, object-cover, group-hover:scale-105 transition
   - Play button overlay: absolute inset-0, bg-black/30, opacity-0 group-hover:opacity-100
   - Labels below image: flex gap-2 with Badge-style spans
     - Style label: bg-primary/10 text-primary px-2 py-1 rounded text-sm
     - Industry label: bg-muted text-muted-foreground px-2 py-1 rounded text-sm

5. **Section CTA** (per CONTEXT.md - repeated CTA for scrollers):
   - Centered Button size="lg" linking to /signup with "Get Started"
   - mt-12 spacing from grid

6. **Section ID**:
   - id="examples" on section element for "See Examples" scroll linking

Import Image from next/image, Button from @/components/ui/button, Link from next/link.
  </action>
  <verify>
File exists at components/marketing/ExampleGallery.tsx.
TypeScript compiles: npx tsc --noEmit
Contains: examples.map to render grid.
Contains: id="examples" on section element.
Contains: style and industry labels with appropriate styling.
  </verify>
  <done>
Example gallery renders 6 example video thumbnails in responsive grid with style/industry labels, hover effects, and bottom CTA.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Marketing Header component</name>
  <files>components/marketing/MarketingHeader.tsx</files>
  <action>
Create MarketingHeader.tsx as a React Server Component with:

1. **Header structure**:
   - header element with border-b for separation
   - Inner container: container mx-auto px-4 py-4
   - flex items-center justify-between layout

2. **Logo** (left side):
   - Link to "/" wrapping Image component
   - Image src="/logo.svg" alt="AnimateLabs" width={150} height={40}
   - No priority needed (logo is small, loads fast)

3. **Navigation** (right side):
   - flex items-center gap-4 (or gap-6 for more spacing)
   - Links styled as text-sm hover:text-primary transition-colors
   - Items (per CONTEXT.md simple navigation):
     - "Pricing" link to /pricing
     - "Login" link to /login
     - "Get Started" Button (size="sm") linking to /signup

4. **Mobile considerations**:
   - Per RESEARCH.md: With only 4 items, keep visible on mobile (no hamburger)
   - May use gap-2 on mobile via gap-2 sm:gap-4
   - Ensure touch targets are 44px minimum (Button handles this)

5. **Styling**:
   - Clean, professional appearance matching shadcn/ui aesthetic
   - Use bg-background for solid header (not transparent)

Import Image from next/image, Link from next/link, Button from @/components/ui/button.
  </action>
  <verify>
File exists at components/marketing/MarketingHeader.tsx.
TypeScript compiles: npx tsc --noEmit
Contains: Link to /pricing, /login, /signup.
Contains: Logo with AnimateLabs alt text.
  </verify>
  <done>
Marketing header renders logo, Pricing link, Login link, and Get Started button with responsive layout.
  </done>
</task>

</tasks>

<verification>
All verification commands must pass:
```bash
# TypeScript compilation
npx tsc --noEmit

# Verify files exist
ls components/marketing/Hero.tsx
ls components/marketing/ExampleGallery.tsx
ls components/marketing/MarketingHeader.tsx
```
</verification>

<success_criteria>
- [ ] Hero.tsx exists with auto-playing video (autoPlay muted loop playsInline)
- [ ] Hero.tsx has headline with price/quality value proposition
- [ ] Hero.tsx has centered "Get Started" CTA linking to /signup
- [ ] ExampleGallery.tsx renders 6 example cards in responsive grid
- [ ] ExampleGallery.tsx shows style and industry labels on each card
- [ ] ExampleGallery.tsx has id="examples" for scroll linking
- [ ] MarketingHeader.tsx has logo linking to /
- [ ] MarketingHeader.tsx has Pricing, Login, Get Started navigation items
- [ ] All components compile without TypeScript errors
- [ ] All components are Server Components (no 'use client')
</success_criteria>

<output>
After completion, create `.planning/phases/07-public-pages-marketing/07-01-SUMMARY.md`
</output>
