---
phase: 07-public-pages-marketing
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/page.tsx
  - components/marketing/SocialProof.tsx
  - components/marketing/PricingSection.tsx
  - components/marketing/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage displays hero section with video and CTA"
    - "Homepage shows example videos gallery"
    - "Homepage has pricing overview with two plan cards"
    - "Homepage has social proof with stats and testimonials"
    - "Homepage has proper SEO metadata for Open Graph"
  artifacts:
    - path: "app/page.tsx"
      provides: "Public homepage with all marketing sections"
      contains: "metadata"
    - path: "components/marketing/SocialProof.tsx"
      provides: "Stats bar and testimonials section"
      contains: "testimonials"
    - path: "components/marketing/PricingSection.tsx"
      provides: "Homepage pricing overview with two cards"
      contains: "Starter"
    - path: "components/marketing/Footer.tsx"
      provides: "Site footer with copyright and links"
      contains: "Animation Labs"
  key_links:
    - from: "app/page.tsx"
      to: "components/marketing/*"
      via: "imports"
      pattern: "from.*components/marketing"
    - from: "components/marketing/PricingSection.tsx"
      to: "/signup"
      via: "Select Plan buttons"
      pattern: "href.*signup"
---

<objective>
Assemble the homepage by creating remaining marketing components (SocialProof, PricingSection, Footer) and integrating all sections into app/page.tsx with proper metadata.

Purpose: Complete the homepage to satisfy PUBL-01 requirement - visitors see hero section, example videos, and pricing overview, enabling conversion to signup.

Output: Fully functional public homepage with SEO metadata, all marketing sections, responsive design.
</objective>

<execution_context>
@/Users/chakaitos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chakaitos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-public-pages-marketing/07-CONTEXT.md
@.planning/phases/07-public-pages-&-marketing**---landing-page-and-pricing/07-RESEARCH.md

# Components from Plan 01
@components/marketing/Hero.tsx
@components/marketing/ExampleGallery.tsx
@components/marketing/MarketingHeader.tsx

# Existing components
@components/ui/card.tsx
@lib/stripe/config.ts
</context>

</skills>
# Required Skills

Use these skills at the appropriate times:

| Skill | When to Use |
|-------|-------------|
| `frontend-design` | When building ANY UI components, pages, or visual elements |
| `llm-application-dev:prompt-engineer` | When generating/optimizing prompts |
| `backend-development:api-design-principles` | When designing ANY API endpoints or routes |

</skills>

<tasks>

<task type="auto">
  <name>Task 1: Create Social Proof component</name>
  <files>components/marketing/SocialProof.tsx</files>
  <action>
Create SocialProof.tsx as a React Server Component with:

1. **Stats bar** (top of section):
   ```typescript
   const stats = [
     { value: '1,000+', label: 'Videos Created' },
     { value: '500+', label: 'Happy Customers' },
     { value: '12 min', label: 'Avg. Delivery Time' },
   ]
   ```
   - grid grid-cols-3 gap-8
   - Each stat: text-center with large value (text-3xl md:text-4xl font-bold) and small label (text-sm text-muted-foreground)

2. **Testimonials** (per CONTEXT.md - real names, roles):
   ```typescript
   const testimonials = [
     {
       quote: "Animation Labs saved my Q4. We needed 20 videos for a product launch and got them all in a single afternoon.",
       author: "Sarah Chen",
       role: "VP of Marketing",
       company: "TechFlow",
     },
     {
       quote: "The quality rivals studios charging 10x the price. Our clients love the intro videos we deliver.",
       author: "Marcus Johnson",
       role: "Founder",
       company: "Creative Labs",
     },
   ]
   ```
   - Section header: h2 "Trusted by Marketing Teams" centered, text-3xl font-bold
   - Grid: grid md:grid-cols-2 gap-6
   - Each testimonial in Card with CardContent p-6
   - Quote in italic text-muted-foreground with surrounding quotes
   - Author details: name (font-semibold), role + company (text-sm text-muted-foreground)

3. **Section styling**:
   - Section: bg-muted/20 for subtle background distinction
   - Container: container mx-auto px-4 py-16
   - Max width: max-w-6xl mx-auto

4. **Note**: Per RESEARCH.md Open Questions - if no real testimonials available, this section can be updated later. For v1, use professional-sounding placeholders that could represent real customers.

Import Card, CardContent from @/components/ui/card.
  </action>
  <verify>
File exists at components/marketing/SocialProof.tsx.
TypeScript compiles: npx tsc --noEmit
Contains: stats array with at least 3 items.
Contains: testimonials array with author and company fields.
  </verify>
  <done>
Social proof component renders stats bar (3 metrics) and testimonials grid (2 testimonials) with professional styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Pricing Section component for homepage</name>
  <files>components/marketing/PricingSection.tsx</files>
  <action>
Create PricingSection.tsx as a React Server Component with:

1. **Section header**:
   - h2: "Simple, Transparent Pricing" centered, text-3xl font-bold mb-4
   - Subtitle: "Pay per video or choose a monthly plan. No hidden fees." text-muted-foreground mb-8

2. **Pricing cards** (side-by-side per CONTEXT.md, simplified from SubscribePlanCard):
   - Container: grid md:grid-cols-2 gap-8 max-w-4xl mx-auto

   **Starter Card:**
   - Card component (not highlighted)
   - CardHeader: title "Starter", description "Perfect for occasional needs"
   - Price: "$30" text-4xl font-bold + "/month" text-muted-foreground
   - Per-video cost: "~$3 per video" text-sm text-muted-foreground
   - CardContent: feature list with Check icons
     - 10 credits/month
     - All animation styles
     - 10-15 min delivery
     - Email support
   - CardFooter: Button variant="outline" "Select Plan" linking to /signup

   **Professional Card:**
   - Card with border-primary shadow-lg and relative positioning
   - "Recommended" badge: absolute -top-3 left-1/2 -translate-x-1/2, bg-primary text-primary-foreground text-xs px-3 py-1 rounded-full
   - CardHeader: title "Professional", description "Best value for regular use"
   - Price: "$75" text-4xl font-bold + "/month" text-muted-foreground
   - Per-video cost: "Only $2.50 per video" text-sm text-muted-foreground
   - CardContent: feature list with Check icons
     - 30 credits/month
     - All animation styles
     - 10-15 min delivery
     - Priority email support
   - CardFooter: Button variant="default" "Select Plan" linking to /signup

3. **Technical guarantee** (per CONTEXT.md):
   - Below cards: text-center mt-8 text-sm text-muted-foreground max-w-2xl mx-auto
   - Text: "Technical Failure Guarantee: If technical issues prevent video delivery, we'll refund your credit. See our extensive examples to know exactly what you'll get."
   - Link "extensive examples" to #examples (scroll link)

4. **Section styling**:
   - Section: bg-muted/30 for subtle background
   - Container: container mx-auto px-4 py-16
   - id="pricing" for potential scroll linking

Import Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter from @/components/ui/card.
Import Button from @/components/ui/button.
Import Check from lucide-react.
Import Link from next/link.
  </action>
  <verify>
File exists at components/marketing/PricingSection.tsx.
TypeScript compiles: npx tsc --noEmit
Contains: Starter and Professional plan cards.
Contains: Per-video cost breakdown ($3, $2.50).
Contains: Technical Failure Guarantee text.
Contains: Links to /signup in both cards.
  </verify>
  <done>
Pricing section renders two side-by-side plan cards with per-video cost, features, and CTAs linking to signup, plus technical guarantee.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Footer and assemble Homepage</name>
  <files>
    components/marketing/Footer.tsx
    app/page.tsx
  </files>
  <action>
**Part A: Create Footer.tsx**

Create simple footer component:
- footer element with border-t mt-16
- Container: container mx-auto px-4 py-8
- Layout: flex flex-col md:flex-row justify-between items-center gap-4
- Left: copyright text "2026 Animation Labs. All rights reserved." text-sm text-muted-foreground
- Right: flex gap-4 text-sm with links to /pricing, /login (hover:text-primary)

Import Link from next/link.

**Part B: Assemble Homepage (app/page.tsx)**

Replace the existing placeholder with complete homepage:

1. **Metadata export** (Next.js 16 pattern):
   ```typescript
   import type { Metadata } from 'next'

   export const metadata: Metadata = {
     title: 'Animation Labs - Professional Logo Animations in Minutes',
     description: 'Create stunning logo animation videos for intros and outros at $3-5 per video. 10-15 minute turnaround with professional quality.',
     openGraph: {
       title: 'Animation Labs - Professional Logo Animations',
       description: 'Professional logo animations at a fraction of traditional cost. $3-5 per video, delivered in minutes.',
       images: ['/og-image.jpg'],
       type: 'website',
     },
     twitter: {
       card: 'summary_large_image',
       title: 'Animation Labs - Professional Logo Animations',
       description: 'Professional logo animations at a fraction of traditional cost.',
     },
   }
   ```

2. **Page structure**:
   ```typescript
   export default function HomePage() {
     return (
       <div className="min-h-screen">
         <MarketingHeader />
         <main>
           <Hero />
           <ExampleGallery />
           <SocialProof />
           <PricingSection />
         </main>
         <Footer />
       </div>
     )
   }
   ```

3. **Imports**:
   - MarketingHeader from @/components/marketing/MarketingHeader
   - Hero from @/components/marketing/Hero
   - ExampleGallery from @/components/marketing/ExampleGallery
   - SocialProof from @/components/marketing/SocialProof
   - PricingSection from @/components/marketing/PricingSection
   - Footer from @/components/marketing/Footer
   - type Metadata from next

4. **Remove old content**: Delete the placeholder that was there before (simple centered Animation Labs heading).

This is a Server Component - no 'use client' directive needed.
  </action>
  <verify>
Files exist:
- components/marketing/Footer.tsx
- app/page.tsx

TypeScript compiles: npx tsc --noEmit

app/page.tsx contains:
- metadata export with title, description, openGraph, twitter
- Imports for all marketing components
- MarketingHeader, Hero, ExampleGallery, SocialProof, PricingSection, Footer components rendered

Footer.tsx contains:
- Copyright text
- Links to pricing and login
  </verify>
  <done>
Homepage assembled with all marketing sections (header, hero, examples, social proof, pricing, footer) and proper SEO metadata.
  </done>
</task>

</tasks>

<verification>
All verification commands must pass:
```bash
# TypeScript compilation
npx tsc --noEmit

# Verify all files exist
ls components/marketing/SocialProof.tsx
ls components/marketing/PricingSection.tsx
ls components/marketing/Footer.tsx
ls app/page.tsx

# Dev server starts without errors
npm run dev &
sleep 5
curl -s http://localhost:3000 | head -50
```
</verification>

<success_criteria>
- [ ] SocialProof.tsx renders stats bar with 3 metrics
- [ ] SocialProof.tsx renders testimonials grid with 2 testimonials
- [ ] PricingSection.tsx has Starter ($30) and Professional ($75) cards
- [ ] PricingSection.tsx shows per-video cost breakdown
- [ ] PricingSection.tsx has technical guarantee messaging
- [ ] PricingSection.tsx cards link to /signup
- [ ] Footer.tsx has copyright and navigation links
- [ ] app/page.tsx has complete metadata export (title, description, openGraph, twitter)
- [ ] app/page.tsx imports and renders all marketing components in correct order
- [ ] Homepage renders without errors at localhost:3000
- [ ] All components are Server Components (no 'use client')
</success_criteria>

<output>
After completion, create `.planning/phases/07-public-pages-marketing/07-02-SUMMARY.md`
</output>
